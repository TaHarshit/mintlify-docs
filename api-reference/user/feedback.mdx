---
title: Feedback
api: 'POST /api/v1/user/feedback'
description: Submit user feedback
---

# Feedback

Submit feedback about the application.

## Endpoint

```
POST /api/v1/user/feedback
```

<Note>This endpoint requires authentication</Note>



## Headers

<ParamField header="X-API-Token" type="string" required>
  Your API access token. You can also set this globally in the API playground.
</ParamField>

<ParamField header="platform" type="string" required>
  The platform making the request: `WEB`, `IOS`, or `ANDROID`.
</ParamField>

<ParamField header="Authorization" type="string" required placeholder="Bearer {token}">
  User authentication token (Bearer token received after signin).
</ParamField>
## Request Body

<ParamField body="feedback" type="string">
  Feedback text
</ParamField>

<ParamField body="rating" type="integer">
  Rating (1-5)
</ParamField>

## Response

<ResponseField name="status" type="string">
  Success status
</ResponseField>

<ResponseField name="data" type="object">
  Response data
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST http://localhost/fss/public/api/v1/user/feedback \
  -H "X-API-Token: YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "feedback": "Great API!",
    "rating": 5
  }'
```

```javascript JavaScript
const response = await fetch('http://localhost/fss/public/api/v1/user/feedback', {
  method: 'POST',
  headers: {
    'X-API-Token': 'YOUR_API_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    feedback: 'Great API!',
    rating: 5
  })
});

const data = await response.json();
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "status": "success",
  "data": {
    "message": "Feedback submitted successfully"
  }
}
```

```json Error Response
{
  "status": "error",
  "message": "Validation failed"
}
```

</ResponseExample>
