---
title: Get User Current Plan
api: 'POST /api/v1/transactions/get_user_current_plan'
description: Get the authenticated user's current subscription plan
---

# Get User Current Plan

Get your current subscription plan.

## Endpoint

```
POST /api/v1/transactions/get_user_current_plan
```

<Note>This endpoint requires authentication</Note>



## Headers

<ParamField header="X-API-Token" type="string" required>
  Your API access token. You can also set this globally in the API playground.
</ParamField>

<ParamField header="platform" type="string" required>
  The platform making the request: `WEB`, `IOS`, or `ANDROID`.
</ParamField>

<ParamField header="Authorization" type="string" required placeholder="Bearer {token}">
  User authentication token (Bearer token received after signin).
</ParamField>
## Request Body

```json
{}
```

## Response

<ResponseField name="status" type="string">
  Success status
</ResponseField>

<ResponseField name="data" type="object">
  Current plan data
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST http://localhost/fss/public/api/v1/transactions/get_user_current_plan \
  -H "X-API-Token: YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{}'
```

```javascript JavaScript
const response = await fetch('http://localhost/fss/public/api/v1/transactions/get_user_current_plan', {
  method: 'POST',
  headers: {
    'X-API-Token': 'YOUR_API_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({})
});

const data = await response.json();
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "status": "success",
  "data": {
    "plan_id": 1,
    "plan_name": "Basic",
    "status": "active",
    "renews_at": "2024-02-15"
  }
}
```

```json Error Response
{
  "status": "error",
  "message": "No active subscription"
}
```

</ResponseExample>
