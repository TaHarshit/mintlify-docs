---
title: Complete Payment
api: 'POST /api/v1/transactions/complete-payment'
description: Complete a subscription payment
---

# Complete Payment

Complete a subscription payment.

## Endpoint

```
POST /api/v1/transactions/complete-payment
```

<Note>This endpoint requires authentication</Note>

## Headers

Authentication is handled via the `X-API-Token` header. You can set this globally in the API playground.

## Request Body

<ParamField body="payment_intent_id" type="string">
  Payment intent ID
</ParamField>

## Response

<ResponseField name="status" type="string">
  Success status
</ResponseField>

<ResponseField name="data" type="object">
  Subscription data
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST http://localhost/fss/public/api/v1/transactions/complete-payment \
  -H "X-API-Token: YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "payment_intent_id": "pi_123456"
  }'
```

```javascript JavaScript
const response = await fetch('http://localhost/fss/public/api/v1/transactions/complete-payment', {
  method: 'POST',
  headers: {
    'X-API-Token': 'YOUR_API_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    payment_intent_id: 'pi_123456'
  })
});

const data = await response.json();
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "status": "success",
  "data": {
    "subscription_id": "sub_123456",
    "status": "active"
  }
}
```

```json Error Response
{
  "status": "error",
  "message": "Payment failed"
}
```

</ResponseExample>
