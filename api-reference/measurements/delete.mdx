---
title: Delete Measurement
api: 'POST /api/v1/measurements/delete'
description: Delete a measurement
---

## Endpoint

```
POST /api/v1/measurements/delete
```

<Note>This endpoint requires authentication</Note>



## Headers

<ParamField header="X-API-Token" type="string" required>
  Your API access token. You can also set this globally in the API playground.
</ParamField>

<ParamField header="platform" type="string" required>
  The platform making the request: `WEB`, `IOS`, or `ANDROID`.
</ParamField>

<ParamField header="Authorization" type="string" required placeholder="Bearer {token}">
  User authentication token (Bearer token received after signin).
</ParamField>
## Request Body

<ParamField body="measurement_id" type="integer">
  measurement id
</ParamField>

## Response



<RequestExample>

```bash cURL
curl --location 'http://fssportal.com/api/v1/measurements/delete' \
--header 'X-API-Token: YOUR_API_TOKEN' \
--header 'Content-Type: application/json' \
--data-raw '{
  "measurement_id": 1
}'
```

```javascript JavaScript
const response = await fetch('http://fssportal.com/api/v1/measurements/delete', {
  method: 'POST',
  headers: {
    'X-API-Token': 'YOUR_API_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
  "measurement_id": 1
})
});

const data = await response.json();
```

</RequestExample>

<ResponseExample>

```json Success Response
null
```

</ResponseExample>

## Notes

- Authentication is required
